<UserControl x:Class="MiniYou_App.Views.MakePostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Créer un post" FontSize="18" FontWeight="SemiBold"/>

        <TextBox Grid.Row="1" Margin="0,10,0,0"
                 Text="{Binding Titre, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBox Grid.Row="2" Margin="0,10,0,0"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Text="{Binding Texte, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Publier" Width="100"
                    Command="{Binding PublishCommand}"/>
            <TextBlock Margin="12,2,0,0" Opacity="0.8"
                       Text="{Binding Status}"/>
        </StackPanel>
    </Grid>
</UserControl>
